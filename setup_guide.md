# 🚀 기도제목 앱 배포 및 셋업 가이드

## 1. Supabase (데이터베이스) 세팅

Supabase 대시보드의 **SQL Editor**에서 아래 SQL 코드를 복사해서 실행(`Run`)합니다:

```sql
-- 1. 파싱된 기도제목을 저장하는 테이블
CREATE TABLE IF NOT EXISTS prayer_requests (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  community TEXT NOT NULL,
  name TEXT NOT NULL,
  content TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 2. 텔레그램에서 버튼을 누르기 전 텍스트를 임시 저장하는 테이블
CREATE TABLE IF NOT EXISTS pending_messages (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  chat_id TEXT NOT NULL,
  text TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 3. 동적 공동체 관리 테이블
CREATE TABLE IF NOT EXISTS communities (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT UNIQUE NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 4. 보안(RLS) 해제
ALTER TABLE prayer_requests DISABLE ROW LEVEL SECURITY;
ALTER TABLE pending_messages DISABLE ROW LEVEL SECURITY;
ALTER TABLE communities DISABLE ROW LEVEL SECURITY;

-- 5. 기본 공동체 목록 채우기
INSERT INTO communities (name) VALUES 
  ('가족'), 
  ('26하GBS'), 
  ('기도후원자'), 
  ('26엘더조'), 
  ('26한사랑국리더십'), 
  ('26한사랑국운영팀'), 
  ('직장')
ON CONFLICT (name) DO NOTHING;
```

---

## 2. 공동체 추가 및 관리 방법

### 방법 1: 웹페이지에서 관리
- 웹페이지 상단 탭 목록 맨 끝의 **`+` 버튼**을 누르면, 모달 창에서 새로운 공동체를 즉시 추가하거나 기존 공동체를 삭제할 수 있습니다.

### 방법 2: 텔레그램 봇에서 관리
- 텔레그램 봇과의 대화창에서 `/추가 [이름]` 명령어를 전송하세요.
  - 예시: `/추가 청년부`
- 전송 즉시 봇과 웹페이지 양쪽에 새로운 공동체 탭이 바로 추가됩니다!
