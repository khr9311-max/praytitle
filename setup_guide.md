# 🚀 기도제목 앱 배포 및 셋업 가이드

코딩이 모두 완료되었습니다! 이제 다음 순서대로 외부 서비스들을 세팅하여 웹사이트를 라이브로 만드실 수 있습니다.

## 1. Supabase (데이터베이스) 세팅

1. [Supabase](https://supabase.com)에 접속해서 회원가입 후 **New Project**를 만듭니다.
2. 프로젝트 대시보드 좌측 메뉴에서 **SQL Editor**로 들어갑니다.
3. 아래의 SQL 코드를 복사해서 붙여넣고 `Run` 버튼을 눌러 테이블을 생성합니다.

```sql
-- 파싱된 기도제목을 저장하는 테이블
CREATE TABLE prayer_requests (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  community TEXT NOT NULL,
  name TEXT NOT NULL,
  content TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 텔레그램에서 버튼을 누르기 전 텍스트를 임시 저장하는 테이블
CREATE TABLE pending_messages (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  chat_id TEXT NOT NULL,
  text TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

4. 좌측 메뉴 톱니바퀴(Project Settings) -> **API** 메뉴에 들어갑니다.
5. **Project URL**과 **Project API keys (anon, public)** 값을 복사해 둡니다.

---

## 2. 텔레그램 봇 만들기

1. 텔레그램 앱에서 **@BotFather**를 검색해서 대화를 시작합니다.
2. `/newbot`을 입력하고 봇의 이름과 아이디(반드시 `bot`으로 끝나야 함)를 정합니다.
3. 생성이 완료되면 제공되는 **HTTP API Token** 값을 복사해 둡니다. (예: `123456789:ABCDefghIJKLmnop...`)

---

## 3. Vercel(버셀)에 배포하기

1. 현재 작업하신 폴더(`d:\기도제목`)를 본인의 **GitHub 저장소(Repository)**에 Push하여 업로드합니다.
2. [Vercel](https://vercel.com)에 가입 후 **Add New... -> Project**를 클릭합니다.
3. 방금 올린 GitHub 저장소를 연결(Import)합니다.
4. 배포 설정 화면의 **Environment Variables(환경 변수)** 섹션을 열고 다음 3가지를 입력합니다.
   - `SUPABASE_URL`: (1-5에서 복사한 Project URL)
   - `SUPABASE_ANON_KEY`: (1-5에서 복사한 anon key)
   - `TELEGRAM_BOT_TOKEN`: (2-3에서 복사한 Bot Token)
5. **Deploy** 버튼을 누르고 배포가 완료될 때까지(1~2분) 기다립니다.
6. 배포가 완료되면 Vercel에서 제공해주는 도메인(URL) 주소를 복사합니다. (예: `https://my-prayer-app.vercel.app`)

---

## 4. 텔레그램 Webhook 연결하기 (마지막 단계)

이제 텔레그램 봇이 메세지를 받으면 방금 만든 Vercel 주소로 데이터를 쏴주도록(Webhook) 연결해야 합니다.

인터넷 브라우저 주소창에 아래 주소를 복사하여 괄호 안의 내용을 채운 뒤 접속(엔터)하세요.

```
https://api.telegram.org/bot(여기에봇토큰)/setWebhook?url=(여기에Vercel주소)/api/telegram
```
- 예시: `https://api.telegram.org/bot1234:ABC/setWebhook?url=https://my-prayer-app.vercel.app/api/telegram`

브라우저 화면에 `{"ok":true,"result":true,"description":"Webhook was set"}` 라고 뜨면 모든 준비가 완료된 것입니다! 🎉

이제 텔레그램 봇에게 텍스트를 보내 테스트해 보세요!

